[project]
name = "docudex"
version = "0.1.0"
description = "Document indexing API"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "flask-sqlalchemy>=3.1",
    "pdfplumber>=0.10",
]

[dependency-groups]
dev = []
